name: alerto24
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  video_player: ^2.4.7
  get:
  firebase_core: ^1.22.0
  # firebase_core: ^2.1.0
  firebase_database: ^9.1.4
  firebase_auth: ^3.8.0
  cloud_firestore: ^3.4.7
  # cloud_firestore: ^4.0.2
  geoflutterfire: ^3.0.3
  google_fonts: ^3.0.1
  provider: 
  google_sign_in:
  font_awesome_flutter: ^10.2.1
  floating_action_bubble: ^1.1.4
  google_maps_flutter: ^2.1.12
  location:
  flutter_svg:
  flutter_polyline_points:
  http: ^0.13.5
  geolocator: ^9.0.1
  flutter_ringtone_player: ^3.2.0
  dropdown_search: ^3.0.1
  flutter_datetime_picker: ^1.5.1
  flutter_launcher_icons: "^0.10.0"
  badges: ^2.0.3
  # vibration: ^1.7.6
  flutter_native_splash: ^2.2.11
  flutter_donation_buttons: ^0.0.5
  url_launcher: ^6.1.6
  camera: ^0.10.0+4
  permission_handler: ^10.2.0
  firebase_storage: ^10.0.2
  image_picker: ^0.8.6
  gallery_saver: ^2.3.2
  youtube_player_flutter: ^8.1.1
  azure_cosmosdb: ^0.9.3
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  
  # flutter_lints: ^2.0.0 #Because alerto24 depends on geoflutterfire ^3.0.3 which depends on flutter_lints ^1.0.0, flutter_lints ^1.0.0 is required.
  flutter_lints: ^1.0.0
  # build_runner: ^2.2.1
  # json_serializable: ^6.4.1

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path_android: "assets/images/AA-logo.png"
  image_path_ios: "assets/images/AA-logo.png"
  min_sdk_android: 21
  remove_alpha_ios: true

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/videos/
